# CI Workflows

- `ci-tests.yml`: suite rapide PR (`main`) + relance manuelle.
- `run-tests.yml`: moteur parametrable Maven (`unit|integration|all`).
- `ci-main-full.yml`: gate principal sur `push main` (`scope=all`).

